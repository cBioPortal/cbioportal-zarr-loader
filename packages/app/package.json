{
  "name": "@cbioportal-zarr-loader/app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:with-docs": "pnpm --filter @cbioportal-zarr-loader/docs build && cp -r ../../docs/build public/docs && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@cbioportal-zarr-loader/zarrstore": "workspace:*",
    "@deck.gl/core": "^9.2.6",
    "@deck.gl/layers": "^9.2.6",
    "@deck.gl/react": "^9.2.6",
    "antd": "^6.2.3",
    "deck.gl": "^9.2.6",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "zustand": "^5.0.11"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "@vitejs/plugin-react": "^5.1.3",
    "jsdom": "^28.0.0",
    "vite": "^7.2.4",
    "vitest": "^4.0.18"
  }
}
