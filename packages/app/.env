# Origin allowed to send postMessage to the app when embedded in an iframe.
# Set to a specific origin in production (e.g. https://www.cbioportal.org).
# "*" accepts messages from any origin (development only).
VITE_POSTMESSAGE_ORIGIN=*
