# Copy this file to .env.local and adjust as needed.
# In production, set these as environment variables directly.

# Base URL for the app (defaults to "/" in vite.config.js)
# VITE_BASE_URL=/

# Allowed postMessage origin when embedded in an iframe.
# Use "*" for development; set to a specific origin in production.
VITE_POSTMESSAGE_ORIGIN=*

# Base URL for the docs site (defaults to "/docs/" in docusaurus.config.js)
# DOCS_BASE_URL=/docs/
